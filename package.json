{
  "name": "gamezone",
  "version": "4.0.0",
  "description": "GameZone — Full-Stack Game Aggregator",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "node --experimental-sqlite --watch server.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "*",
    "dotenv": "*",
    "express": "*",
    "multer": "*",
    "pg": "^8.20.0",
    "socket.io": "*"
  }
}
